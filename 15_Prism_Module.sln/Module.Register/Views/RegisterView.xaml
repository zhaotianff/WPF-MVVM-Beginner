<UserControl x:Class="Module.Register.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Module.Register.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Content="Id" HorizontalAlignment="Right" Margin="0,0,30,0" VerticalAlignment="Center"></Label>
        <TextBox Grid.Row="0" Grid.Column="1" Margin="40,0" VerticalAlignment="Center"  Text="{Binding EmployeeViewModel.Id}"></TextBox>

        <Label Content="Name" Grid.Row="1" HorizontalAlignment="Right" Margin="0,0,30,0" VerticalAlignment="Center"></Label>
        <TextBox Grid.Row="1" Grid.Column="1" Margin="40,0"  VerticalAlignment="Center"  Text="{Binding EmployeeViewModel.Name}"></TextBox>

        <Label Content="Phone" Grid.Row="2" HorizontalAlignment="Right" Margin="0,0,30,0" VerticalAlignment="Center"></Label>
        <TextBox Grid.Row="2" Grid.Column="1" Margin="40,0"  VerticalAlignment="Center" Text="{Binding EmployeeViewModel.Phone}"></TextBox>

        <Button Content="登记" Width="88" Height="28" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="3" Grid.ColumnSpan="2" Command="{Binding RegisterCommand}"></Button>
    </Grid>
</UserControl>
